# Closed ideas (dedup list)

This file is the dedup list + the log of ideas the loop closed. The idea miner
reads it so it doesn't re-file something already tried, and the runner appends a
line here when it closes an idea as NULL or rejects it.

## Closed by the loop

<!-- reviewer/evidence step appends one line per close here -->
